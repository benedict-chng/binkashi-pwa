---
phase: 03-polish-user-experience
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [src/components/BinList.tsx, src/components/BinCard.tsx, src/components/BinForm.tsx, src/App.tsx, src/components/EmptyState.tsx, src/components/LoadingState.tsx, src/index.css]
autonomous: false
requirements: []

must_haves:
  truths:
    - User sees smooth transitions when interacting with app
    - Typography is consistent and readable across all screens
    - Empty states guide users to take action
    - Loading states provide clear feedback
    - App feels polished and professional
  artifacts:
    - path: "src/components/EmptyState.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState"]
    - path: "src/components/LoadingState.tsx"
      provides: "Reusable loading state component"
      exports: ["LoadingState"]
    - path: "src/index.css"
      provides: "Global CSS transitions and animations"
      contains: "transition classes, keyframe animations"
    - path: "src/components/BinCard.tsx"
      provides: "Smooth hover and click transitions"
      contains: "transition classes, hover effects"
  key_links:
    - from: "src/components/BinList.tsx"
      to: "src/components/EmptyState.tsx"
      via: "empty state when no bins exist"
      pattern: "EmptyState"
    - from: "src/components/BinList.tsx"
      to: "src/components/LoadingState.tsx"
      via: "loading state when bins undefined"
      pattern: "LoadingState"
    - from: "All components"
      to: "src/index.css"
      via: "CSS transitions and animations"
      pattern: "transition"
---

<objective>
Polish user interface with smooth transitions, consistent design, and improved UX states

Purpose: Create a polished, professional feel with smooth animations, consistent typography/spacing, helpful empty states, clear loading indicators, and enhanced accessibility

Output: Refined UI with smooth transitions, consistent design system, empty/loading states, and professional polish
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
@$HOME/.config/opencode/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase patterns
From Phase 2:
- Tailwind CSS 4.2 for styling (src/index.css, tailwind.config.js)
- Toast notifications (src/components/Toast.tsx)
- Responsive layout with 44px touch targets
- Mobile-first design

From Phase 1:
- React + TypeScript setup
- Bin list and form components

# Design tokens (from existing code)
- Primary color: green-600 (composting theme)
- Typography: Tailwind default (font-sans)
- Spacing: Tailwind scale (4 = 1rem, 6 = 1.5rem, etc.)
- Border radius: rounded-md, rounded-lg
- Shadows: shadow-sm, shadow-md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS transitions and animations</name>
  <files>src/index.css</files>
  <action>
Add smooth transitions and animations to global CSS:

1. **Base transitions**:
   - Add transition classes for common interactions
   - duration: 200-300ms (fast but smooth)
   - easing: ease-in-out (natural feel)
   - Properties: transform, opacity, color, background-color

2. **Button hover transitions**:
   - Add scale effect (hover: scale-105)
   - Add color transition
   - Ensure touch targets remain stable on mobile

3. **Card hover transitions**:
   - Add subtle shadow increase on hover
   - Add lift effect (transform: translateY(-2px))
   - Add smooth border color change

4. **Form input transitions**:
   - Add focus ring animation
   - Add border color transition
   - Add background color transition

5. **Modal transitions**:
   - Add fade-in animation
   - Add scale-in animation
   - Add backdrop blur transition

6. **Toast animations**:
   - Add slide-in animation (from top-right)
   - Add fade-out animation before dismiss
   - Add smooth height transition for toasts

Use Tailwind utility classes where possible. Add custom keyframes in index.css for complex animations.
  </action>
  <verify>
CSS file has transition classes and animations. All interactive elements have smooth transitions. Animations not jarring or slow.
  </verify>
  <done>Smooth transitions added to all interactive elements</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable EmptyState component</name>
  <files>src/components/EmptyState.tsx, src/components/BinList.tsx</files>
  <action>
Create reusable EmptyState component and update BinList to use it:

1. **EmptyState component**:
   - Props: icon (React element), title (string), message (string), action (optional button)
   - Centered layout with icon, title, message
   - Optional action button (e.g., "Create First Bin")
   - Subtle animation (fade-in, slight scale)
   - Use consistent spacing and typography

2. **Update BinList**:
   - Replace existing empty state with EmptyState component
   - Pass appropriate icon (e.g., 🗑️ or custom SVG icon)
   - Title: "No bins yet"
   - Message: "Create your first bin to get started"
   - Action: Link to /bins/new with "Create First Bin" button

3. **Icon choice**:
   - Use emoji or SVG icon
   - Keep it simple and relevant to bins/composting
   - Ensure icon is accessible (aria-label or alt text)

EmptyState should feel inviting and guide users to take action.
  </action>
  <verify>
EmptyState component exists and is exported. BinList uses EmptyState for no bins case. Action button navigates to /bins/new. Accessible with proper labels.
  </verify>
  <done>Empty states guide users to take action</done>
</task>

<task type="auto">
  <name>Task 3: Create reusable LoadingState component</name>
  <files>src/components/LoadingState.tsx, src/components/BinList.tsx, src/components/BinForm.tsx</files>
  <action>
Create reusable LoadingState component and update components to use it:

1. **LoadingState component**:
   - Props: message (optional string), size ('small' | 'medium' | 'large')
   - Spinner animation (CSS keyframe rotation)
   - Optional message below spinner
   - Centered layout
   - Use consistent sizing (small: 24px, medium: 48px, large: 64px)

2. **Update BinList**:
   - Replace "Loading bins..." with LoadingState component
   - Use medium size
   - Optional message: "Loading bins..."

3. **Update BinForm**:
   - Add loading state during form submission
   - Show LoadingState (small) below submit button or as overlay
   - Disable submit button during loading
   - Show "Saving..." message

4. **Spinner animation**:
   - Use CSS keyframe rotation
   - Consistent color (green-600 primary)
   - Smooth animation (1s duration, linear)

LoadingState should provide clear feedback that something is happening.
  </action>
  <verify>
LoadingState component exists and is exported. BinList and BinForm use LoadingState. Spinner animation smooth. Size prop works correctly.
  </verify>
  <done>Loading states provide clear feedback to users</done>
</task>

<task type="auto">
  <name>Task 4: Polish typography and spacing consistency</name>
  <files>src/components/BinList.tsx, src/components/BinCard.tsx, src/components/BinForm.tsx, src/App.tsx</files>
  <action>
Ensure consistent typography and spacing across all components:

1. **Typography**:
   - Headings: Use consistent font weights (bold for h1, semibold for h2, etc.)
   - Body text: Use font-normal with consistent line-height (leading-6)
   - Small text: Use text-sm or text-xs consistently
   - Text colors: Use text-gray-900 for primary, text-gray-600 for secondary, text-gray-400 for hints

2. **Spacing**:
   - Section spacing: space-y-6 or space-y-8 for vertical rhythm
   - Card spacing: p-6 for card padding, gap-4 for internal spacing
   - Form spacing: space-y-4 between form fields
   - Button spacing: px-6 py-3 for primary buttons, px-4 py-2 for secondary

3. **Visual hierarchy**:
   - Ensure clear hierarchy with size and weight differences
   - Use color to indicate hierarchy (green-600 for primary actions, gray for secondary)
   - Ensure contrast ratios meet WCAG AA standards

4. **Specific component polish**:
   - BinCard: Consistent spacing between elements, proper alignment
   - BinForm: Clear label/input associations, consistent field spacing
   - BinList: Proper grid gaps, consistent card sizes
   - App header: Proper padding, alignment

Audit all components for consistency. Fix any inconsistencies.
  </action>
  <verify>
Typography consistent across all components. Spacing follows consistent scale. Visual hierarchy clear. No random spacing values.
  </verify>
  <done>Typography and spacing consistent across app</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify visual polish and UX improvements</name>
  <what-built>Complete visual polish with transitions, empty/loading states, and consistent design</what-built>
  <how-to-verify>
    1. Open app at http://localhost:5173
    2. Test empty state: Delete all bins, verify EmptyState appears with action button
    3. Test loading state: Refresh page, verify LoadingState appears briefly
    4. Test transitions: Hover over bins, buttons, form inputs - verify smooth animations
    5. Test form: Create new bin, verify loading state during save
    6. Test consistency: Check all screens for consistent typography and spacing
    7. Test accessibility: Check keyboard navigation and screen reader labels
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Visual quality checklist:
1. Transitions smooth and natural (200-300ms, ease-in-out)
2. Empty states guide users to take action
3. Loading states provide clear feedback
4. Typography consistent (font sizes, weights, line-heights)
5. Spacing consistent (4px scale, proper vertical rhythm)
6. Visual hierarchy clear (size, weight, color)
7. No jarring or distracting animations
8. Accessible (keyboard navigation, screen reader labels)
</verification>

<success_criteria>
1. All interactive elements have smooth transitions
2. Empty states are helpful and guide users to take action
3. Loading states provide clear feedback during async operations
4. Typography and spacing are consistent across all screens
5. App feels polished and professional
6. User approves visual quality (checkpoint verified)
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-user-experience/03-03-SUMMARY.md`
</output>
