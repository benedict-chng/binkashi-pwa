---
phase: 02-image-handling-user-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/types/bin.ts
  - src/hooks/useBinActions.ts
  - src/components/BinForm.tsx
autonomous: true
requirements: [IMG-01, IMG-02]

must_haves:
  truths:
    - User can capture a bin photo using their phone's camera
    - User can upload a bin image from their device's file picker/gallery
    - Captured/uploaded images are stored in IndexedDB as Blobs
    - Images persist across app restarts and offline usage
    - Image field is optional (bins can exist without images)
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Bin schema with image Blob field"
      contains: "image: Blob | null"
    - path: "src/types/bin.ts"
      provides: "Type definitions with image field"
      exports: ["Bin", "BinFormData"]
    - path: "src/components/BinForm.tsx"
      provides: "Image capture UI with camera and file inputs"
      exports: ["BinForm"]
    - path: "src/hooks/useBinActions.ts"
      provides: "CRUD operations with Blob handling"
      exports: ["createBin", "updateBin"]
  key_links:
    - from: "src/components/BinForm.tsx"
      to: "src/hooks/useBinActions.ts"
      via: "Form submission with File object"
      pattern: "createBin|updateBin.*image"
    - from: "src/hooks/useBinActions.ts"
      to: "src/db/schema.ts"
      via: "Dexie put() with Blob"
      pattern: "image.*Blob"
---

<objective>
Implement camera capture and file upload for bin images with Blob storage in IndexedDB

Purpose: Enable users to attach photos to bins for visual tracking and identification. Images are stored locally in IndexedDB as Blobs, supporting offline usage without external dependencies.

Output: Updated Bin schema with image field, BinForm with capture/upload UI, and CRUD operations that handle Blob storage
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pwa-foundation-core-bin-management/01-01-SUMMARY.md
@.planning/phases/01-pwa-foundation-core-bin-management/01-02-SUMMARY.md
@.planning/phases/01-pwa-foundation-core-bin-management/01-03-SUMMARY.md

<interfaces>
<!-- Existing Bin interface (from src/db/schema.ts) -->
```typescript
export interface Bin {
  id?: number;
  name: string;
  state: 'Empty' | 'In Use' | 'Fermenting';
  inUseStartDate: Date | null;
  fermentingStartDate: Date | null;
  createdAt: Date;
  updatedAt: Date;
}
```

<!-- Existing BinFormData interface (from src/types/bin.ts) -->
```typescript
export interface BinFormData {
  name: string;
  state: BinState;
  inUseStartDate: Date | null;
  fermentingStartDate: Date | null;
}
```

<!-- Existing useBinActions exports (from src/hooks/useBinActions.ts) -->
```typescript
export const useBinActions = () => ({
  createBin: (data: BinFormData) => Promise<number>,
  updateBin: (id: number, data: BinFormData) => Promise<void>,
  deleteBin: (id: number) => Promise<void>
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image field to Bin schema and types</name>
  <files>src/db/schema.ts, src/types/bin.ts</files>
  <action>
Add optional image Blob field to Bin interface and related types:

1. In src/db/schema.ts:
   - Add `image: Blob | null;` to Bin interface
   - Update Dexie stores to include image in index: `'++id, name, state, inUseStartDate, fermentingStartDate, createdAt, updatedAt, image'`

2. In src/types/bin.ts:
   - Add `image?: File | Blob | null;` to BinFormData interface (File for form input, Blob for storage)
   - Ensure Bin type extends DBBin automatically (no manual update needed)

Keep image optional - bins can exist without images. This maintains backward compatibility with existing data.
  </action>
  <verify>
grep -n "image.*Blob" src/db/schema.ts && grep -n "image.*File.*Blob" src/types/bin.ts
  </verify>
  <done>Bin schema and types include optional image field for Blob storage</done>
</task>

<task type="auto">
  <name>Task 2: Add image capture UI to BinForm</name>
  <files>src/components/BinForm.tsx</files>
  <action>
Add camera capture and file upload inputs to BinForm component:

1. Add image state management:
   - Update BinFormData interface to include image field (already done in Task 1)
   - Add image preview state: `const [imagePreview, setImagePreview] = useState<string | null>(null);`
   - Handle file selection with preview: `URL.createObjectURL(file)`

2. Add two input elements below existing fields:
   - Primary file picker: `<input type="file" accept="image/*" onChange={handleFileChange} />`
   - Camera capture: `<input type="file" accept="image/*" capture="environment" onChange={handleFileChange} />`
   - Use separate buttons with labels: "Upload Photo" and "Take Photo"

3. Add image preview display:
   - Show selected image preview above form fields
   - Include "Remove Image" button to clear selection
   - Use Tailwind classes for responsive preview: `max-w-full h-48 object-cover rounded-md`

4. Form submission:
   - Include image File object in formData when calling createBin/updateBin
   - Clear image preview with URL.revokeObjectURL() after successful submission

Note: Safari iOS may not support both capture and accept on same input. Use two separate input elements to maximize compatibility.
  </action>
  <verify>
grep -n "type=\"file\".*accept=\"image/.*" src/components/BinForm.tsx && grep -n "capture=\"environment\"" src/components/BinForm.tsx
  </verify>
  <done>BinForm has working camera capture and file upload inputs with preview</done>
</task>

<task type="auto">
  <name>Task 3: Handle image Blob storage in useBinActions</name>
  <files>src/hooks/useBinActions.ts</files>
  <action>
Update useBinActions hook to store and retrieve image Blobs:

1. Update createBin function:
   - Accept `image?: File | Blob | null` in data parameter
   - If image is a File (from form input), convert to Blob: File is already a Blob type
   - Store image in IndexedDB via Dexie put operation
   - Return the created bin ID

2. Update updateBin function:
   - Accept `image?: File | Blob | null` in data parameter
   - Handle image changes:
     - If new image provided: store it (convert File to Blob if needed)
     - If image is null or undefined: remove existing image from bin
     - If image field not provided: keep existing image unchanged
   - Use Dexie update operation to merge changes

3. Blob handling:
   - Blobs store directly in IndexedDB without base64 conversion
   - No compression in this task (address in later plan if needed)
   - File objects are already Blob type in TypeScript, direct assignment works

4. Ensure updatedAt timestamp is set automatically for both create and update operations (already implemented)

Implementation pattern: `await db.bins.put({ id, ...data, image: data.image as Blob | null, updatedAt: new Date() })`
  </action>
  <verify>
grep -n "image.*Blob" src/hooks/useBinActions.ts && grep -n "createBin\|updateBin" src/hooks/useBinActions.ts | head -5
  </verify>
  <done>useBinActions stores and retrieves image Blobs correctly from IndexedDB</done>
</task>

</tasks>

<verification>
1. Test camera capture: On mobile, click "Take Photo" → camera opens → capture → image appears in preview
2. Test file upload: Click "Upload Photo" → select from gallery → image appears in preview
3. Test storage: Create bin with image → refresh page → navigate to edit → image loads correctly
4. Test persistence: Create bin with image → close app → reopen → image still displays
5. Test optional image: Create bin without image → bin saves successfully with no image field
6. Test remove image: Edit bin with image → click "Remove Image" → save → image removed from database
</verification>

<success_criteria>
- Camera capture works on mobile devices (file input with capture="environment")
- File upload works for gallery images
- Images are stored as Blobs in IndexedDB and persist across sessions
- Image field is optional and bins work without images
- Image previews display correctly in the form
- Remove image button clears the image selection
</success_criteria>

<output>
After completion, create `.planning/phases/02-image-handling-user-interface/02-01-SUMMARY.md`
</output>
